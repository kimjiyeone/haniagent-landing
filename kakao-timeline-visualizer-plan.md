# 카카오맵 기반 타임라인 비주얼라이저 — 분석 및 기획

> 레퍼런스: [mahlernim/google-timeline-visualizer](https://github.com/mahlernim/google-timeline-visualizer) (MIT License, v2.1.1)
> 작성일: 2026-08-19

---

## 0. 3줄 요약

1. 원본은 **Google Timeline JSON(데이터) + CARTO/OSM 타일(지도) + 온디바이스 MP4 렌더링(영상)** 세 축으로 구성된 안드로이드 앱이다. 지도는 전체 구조에서 **가장 얇은 레이어**다.
2. **카카오맵에는 타임라인(이동 기록) 기능 자체가 없다.** 따라서 "카카오맵 기반"은 데이터 소스를 바꾸는 게 아니라 **지도 표현 레이어를 바꾸는 것**이며, 데이터는 여전히 Google Timeline JSON(또는 GPX·사진 EXIF)에서 와야 한다. 이게 이 기획의 가장 중요한 제약이다.
3. 권장 방향: **웹 우선(Web-first) + 100% 클라이언트 사이드 처리 + 다중 입력 소스 + 한국 특화 통계/세로형 숏폼**. 앱 설치 마찰이 없어 바이럴에 유리하고, 위치정보법 리스크도 회피된다.

> **2026-08-19 개정**: 구글 타임라인을 꺼둔 사용자가 다수라는 점이 확인되어 **입력 소스 다변화를 P0으로 상향**했다. 상세는 [§2.2 데이터 소스 실전 가이드](#22-데이터-소스-실전-가이드-사용자용) 참조.

---

## 1. 원본 서비스 분석

### 1.1 구성

| 구성요소 | 내용 |
|---|---|
| Android 앱 | Kotlin 45개 파일, 약 7,911줄. 실질 제품 |
| Python CLI | `visualizer.py` 678줄. 원조 프로토타입이자 알고리즘 레퍼런스 |
| 지도 | CARTO `light_all` 래스터 타일 (OSM 기반), z2~z15 |
| 배포 | Google Play 미등록, GitHub Releases APK 직접 배포 |
| 라이선스 | MIT (포크·상업 이용 가능, 저작자 표시 필요) |

### 1.2 파이프라인 (7단계)

```
① Timeline.json 선택
    ↓
② 파싱 → GeoPoint(시각, 위도, 경도) 리스트        [TimelineParser.kt / extract_timeline_points]
    ↓
③ GPS 이상치 필터                                  [LocationOutlierFilter.kt]
    ↓
④ 기간 필터 → Journey(누적거리 배열 cum_dist)      [TimelineModels.kt]
    ↓
⑤ 타이밍 곡선 + 카메라 트랙 계산                    [JourneyTiming.kt / CameraSettings.kt]
    ↓
⑥ 프레임별 지도 타일 합성 + 경로 드로잉             [TimelinePainter.kt, 781줄 — 핵심]
    ↓
⑦ MediaCodec H.264 인코딩 → MediaMuxer → MP4       [Mp4Exporter.kt]
```

### 1.3 알고리즘 핵심 (그대로 이식 가치가 있는 부분)

**a) 거리 기반 애니메이션 타이밍 — "장거리 압축"**

경로 재생 속도를 시간이 아니라 **누적 이동거리**에 매핑한다. 그대로 하면 비행기 한 번에 영상의 80%를 쓰게 되므로, 구간 거리를 지수로 눌러 압축한다.

```
effective = Σ (segment_km ^ exponent)
exponent: off 1.00 / gentle 0.92 / balanced 0.85(기본) / strong 0.75
```
그리고 `progress → distance` 매핑을 **monotone cubic Hermite 보간**(Fritsch–Carlson)으로 만들어 속도가 튀지 않게 한다. 경로 모양은 건드리지 않고 **재생 시간만** 바꾸는 게 설계 포인트다.

**b) 카메라 3종**

| 모드 | context 비율 | min~max context(km) | padding | zoom-out α | zoom-in α | leg 인식 | 고정 줌 |
|---|---|---|---|---|---|---|---|
| FIXED | 0.10 | 25 ~ 350 | 2.6 | 0 | 0 | ✗ | ✓ (80퍼센타일 span 고정) |
| STEADY(기본) | 1.00 | 650 ~ 650 | 2.8 | 0.14 | 0.035 | ✗ | ✗ |
| DYNAMIC | 0.10 | 100 ~ 350 | 2.2 | 0.24 | 0.06 | ✓ | ✗ |

- 480개 샘플로 카메라 트랙 선계산 후, **로그 공간 EMA**로 줌 스무딩 (줌아웃이 줌인보다 4배 빠름 → 시야를 먼저 넓히고 천천히 좁힌다)
- **데드존 0.20**: 마커가 화면 중앙 ±20% 안에서는 카메라가 안 움직임 → 출퇴근 경로에서 지도가 덜덜 떠는 현상 제거. 이게 체감 품질을 가장 크게 좌우한다.
- **Leg 감지**: hop 거리의 중앙값 × 3 또는 median + MAD×6 (60~120km로 클램프) 초과 구간을 "장거리 이동(비행)"으로 판정해 그 구간 전체를 한 화면에 담는다.

**c) 대권(great-circle) 보간**

인천→호놀룰루처럼 중간 점이 없는 구간을 slerp로 보간해 카메라가 순간이동하지 않고 호를 그리며 따라간다. 날짜변경선 통과도 처리.

**d) 꼬리(trail) 페이드**

지나간 경로는 옅게(alpha 0.5) 남기고, 최근 구간만 굵게. 꼬리 길이는 **2.5초 분량의 이동거리**를 기준으로 80~2,000km 사이 동적 계산. 점 수가 많아도 렌더 비용이 폭발하지 않는 이유.

**e) 아웃트로**

경로 재생 종료 후 **1.5초** 동안 전체 경로로 줌아웃(전환 1.0초) + 0.5초 정지. 1080×1080 전체 경로 스틸 이미지도 별도 저장/공유 가능.

**f) GPS 이상치 필터 (보수적)**

"왕복이 물리적으로 불가능한 고립점"만 제거. 조건이 매우 빡빡하다 — 최대 3점 연속, 진입/이탈 각 500km 이상, 속도 1,300km/h 초과, 12시간 이내 200km 이내로 복귀, 클러스터 지름 200km 이내. 원본 JSON은 건드리지 않고 제거 개수만 표시.

**g) 파서 호환성**

`semanticSegments` 신규/구형, `timelinePath`/`activity`/`visit` 3종 세그먼트, 좌표 표기 5종(문자열, `latLng`, 도(°) 표기, `geo:` URI, E7 정수), 절대 시각 및 시작시각+분오프셋. **여기가 실무에서 가장 지저분하고, 그대로 가져올 가치가 가장 큰 코드다.**

### 1.4 제품 스펙

- 영상 길이 10~300초 (기본 30초, 프리셋 10/15/20/30/45/60)
- 화질 480p / 720p / 1080p — **정사각형(1:1)만 지원**, 24fps
- 제목 템플릿 `{year}`, `{name}`
- 내 영상 라이브러리(썸네일·메타 로컬 인덱스), 외부 MP4 가져오기, 인앱 플레이어
- 9개 언어, 백그라운드 렌더링(포그라운드 서비스), 진행률·취소·알림
- 프라이버시: 구글 로그인 없음, 위치 권한 없음, 애널리틱스 없음. **네트워크는 지도 타일에만 사용**

### 1.5 원본의 약점 = 우리의 기회

| 약점 | 기회 |
|---|---|
| 안드로이드 APK 사이드로딩만 가능 (Play 미등록) | **웹으로 만들면 설치 마찰 0** — 링크 하나로 바이럴 |
| 아이폰 사용자는 JSON을 안드로이드로 옮겨야 함 | 웹이면 기기 무관 |
| 1:1 정사각형만 | **9:16 세로형** = 릴스/쇼츠/스토리 |
| OSM 기반이라 국내 지명·도로 표현이 빈약 | 카카오맵 = 국내 최적화 지도 |
| 통계 오버레이 없음 (제목 + 연월만) | 방문 시·도 수, 총 이동거리, 국토 커버리지 % 등 |
| 사진 연동 없음 | 사진 EXIF 연동 → 여행 영상으로서 훨씬 강력 |

---

## 2. 핵심 제약: "카카오맵 기반"이 성립하지 않는 부분

> **카카오맵에는 이동 기록(타임라인)을 저장하거나 내보내는 기능이 없다.**

국내 지도 앱은 OS 레벨 백그라운드 위치 로깅을 상시 수행하지 않는다. 카카오맵의 "즐겨찾기/내 장소"는 사용자가 직접 저장한 POI일 뿐 이동 경로가 아니고, 공개 내보내기 API도 없다. 카카오내비 주행 이력도 마찬가지다.

따라서 선택지는 셋뿐이며, **A와 B를 함께 채택**한다.

| 안 | 데이터 소스 | 평가 |
|---|---|---|
| **A. 지도만 교체** | Google Timeline JSON 유지 | ✅ 채택. 원본 대비 즉시 우위(웹·세로형·국내지도·통계) 확보 |
| **B. 입력 다변화** | 사진 EXIF, 운동 GPX, GPX/TCX | ✅ **A와 함께 P0으로 채택.** 타임라인을 꺼둔 사용자가 다수 → A만으로는 진입장벽이 너무 높다 |
| C. 자체 위치 수집 | 직접 백그라운드 로깅 앱 | ❌ MVP 부적합. 데이터가 쌓이는 데 수개월, **위치정보법상 위치기반서비스사업 신고** 대상, 배터리·권한 이슈 |

### 2.1 데이터 소스 상세 비교

| 소스 | 플랫폼 | 획득 난이도 | 데이터 밀도 | 과거 데이터 | 포맷 | 우선순위 |
|---|---|---|---|---|---|---|
| Google Timeline JSON | And/iOS | 중 | ★★★★★ 연속 경로 | 켜뒀다면 수년치 | JSON | **P0** |
| **사진 EXIF GPS** | 전부 | **하** | ★★☆ 촬영 지점만 | **이미 수년치 존재** | JPEG/HEIC | **P0** |
| **Apple 건강 앱 운동 경로** | iOS | 하 | ★★★★★ (운동 세션 한정) | 애플워치 사용자면 수년치 | GPX | **P0** |
| GPX/TCX (스트라바·나이키런·삼성헬스) | 전부 | 하 | ★★★★★ (세션 한정) | 앱 사용 이력만큼 | GPX/TCX | P1 |
| Arc Timeline 등 iOS 로거 | iOS | 중 | ★★★★★ 연속 경로 | **없음(설치 후부터)** | GPX/JSON | P1 |
| 카카오맵 즐겨찾기 | 전부 | 하(수동) | ★ 점 단위 | 저장한 만큼 | 수동 | P2 |
| 직접 백그라운드 로깅 | 자체 앱 | 상 | ★★★★★ | 없음 | - | ❌ |

**설계 결론 — 입력 어댑터 패턴**

모든 소스를 `GeoPoint { instant, lat, lon, source? }[]` 하나로 정규화하고, 그 뒤 파이프라인(이상치 필터 → Journey → 타이밍 → 카메라 → 렌더)은 소스를 전혀 모르게 만든다. 소스 추가가 어댑터 1개 추가로 끝나야 한다.

```
sources/googleTimeline.ts ─┐
sources/gpx.ts             ├─→ normalize() ─→ GeoPoint[] ─→ [기존 파이프라인]
sources/exif.ts            ┘
```

**희소 데이터(사진 EXIF) 대응**

사진 EXIF는 점 데이터라 원본의 연속 경로 전제가 깨진다. 다음 보정이 필요하다.

- 원본에 이미 있는 **대권 보간**으로 점과 점 사이를 잇는다 (인천→방콕 같은 큰 점프에 이미 대응됨)
- **체류 판정**: 같은 지역에서 여러 장 찍힌 사진을 하나의 "방문"으로 묶어 카메라가 그 자리에 머물게 한다
- **Leg 임계값 재조정**: 원본은 hop 중앙값 기반(60~120km 클램프)인데, 사진 데이터는 hop 중앙값 자체가 커서 임계값을 소스별로 다르게 잡아야 한다
- **보너스**: 지도 위에 사진 썸네일을 띄울 수 있다 → 원본에 없는 강력한 차별화

### 2.2 데이터 소스 실전 가이드 (사용자용)

제품 내 "데이터 준비" 화면에 그대로 들어갈 내용.

#### ① 구글 타임라인을 껐더라도 — 먼저 확인부터

껐다는 건 "이후로 기록 안 함"이지 "기존 기록 삭제"가 아니다. 끄기 전 데이터는 남아 있을 수 있다.

- **아이폰**: 구글 지도 앱 → 프로필 → 설정 → 개인 콘텐츠 → **타임라인 데이터 내보내기**
- **안드로이드**: 설정 → 위치 → 위치 서비스 → 타임라인 → 타임라인 데이터 내보내기
- **웹**: [takeout.google.com](https://takeout.google.com) → "위치 기록(타임라인)" 선택
- ⚠️ 2024년 12월부터 타임라인이 **기기 저장 방식**으로 바뀌었다. 그 이후 기기를 바꿨다면 예전 기기 데이터는 그 기기에만 있을 수 있다.

#### ② 사진 EXIF — 껐어도 이미 데이터가 있는 유일한 소스

아이폰은 카메라 위치 권한이 켜져 있으면 **모든 사진에 촬영 좌표와 시각을 자동 기록**한다. 여행 사진이 있다면 이미 몇 년치 여행 경로를 갖고 있는 셈이다.

- **아이폰에서 내보내기**: 사진 앱 → 사진 여러 장 선택 → 공유 → 상단 **"옵션"** → **위치 정보 ON**, **모든 사진 데이터 ON** → 파일에 저장 / AirDrop
  - ⚠️ 이 옵션을 안 켜면 iOS가 위치를 **지우고** 내보낸다. 여기서 대부분 실패한다.
- **Mac에서**: 사진 앱 → 파일 → 내보내기 → **"미변경 원본 내보내기"**
- 장점: 획득이 제일 쉽고, 과거 데이터가 이미 있고, 여행 영상과 궁합이 제일 좋다
- 단점: 점 데이터 (§2.1의 희소 데이터 대응 필요)

#### ③ Apple 건강 앱 — 아이폰 유저의 숨은 GPS 창고

애플워치나 아이폰으로 야외 운동(달리기·산책·자전거·하이킹)을 기록했다면 **GPX 경로가 통째로 저장돼 있다.**

- 건강 앱 → 우측 상단 프로필 → **모든 건강 데이터 내보내기** → `export.zip`
- 압축 해제 → `apple_health_export/**workout-routes/**` 폴더 안에 `*.gpx` 파일 다발
- 밀도가 매우 높은 진짜 GPS 트랙. 단, **운동한 구간만** 존재

#### ④ 지금부터 쌓기 시작할 거라면

- **구글 타임라인 다시 켜기** — 가장 단순. 단 오늘부터 쌓임
- **Arc Timeline** (iOS) — 온디바이스 위치 기록 + GPX 내보내기. 자동 이동/장소 인식이 강점이나 업데이트가 뜸함
- **OwnTracks** — 오픈소스, 자체 서버 운영형
- **스트라바 / 나이키런 클럽** — 운동 한정이지만 GPX 내보내기 확실

#### ⑤ 안 되는 것 (확인 완료)

- ❌ **iOS "중요 위치"** (설정 → 개인정보 보호 → 위치 서비스 → 시스템 서비스 → 중요 위치): 화면으로 볼 수만 있고 **내보내기·서드파티 API 없음.** 온디바이스 암호화 + 이중 잠금
- ❌ **카카오맵 / 네이버 지도**: 이동 기록 기능 자체가 없음
- ❌ **애플 지도 "최근 항목"**: 검색 이력이지 경로가 아님

#### 사용자별 추천 조합

| 상황 | 추천 |
|---|---|
| 구글 타임라인을 켜둔 적 있음 | ① 먼저 시도 |
| 껐지만 여행 사진은 많음 | **② 사진 EXIF** ← 지금 사용자 케이스 |
| 애플워치로 운동 기록함 | ③ + ② 병합 |
| 앞으로를 위해 | ④ 로거 설치 + ②로 과거 채우기 |

**여러 소스를 동시에 올려서 병합**할 수 있게 만드는 게 핵심 UX다. 사진 EXIF로 뼈대를 잡고 운동 GPX로 살을 붙이면 꽤 그럴듯한 경로가 나온다.

---

## 3. 지도 레이어 선택 — 가장 중요한 기술 의사결정

원본은 CARTO 타일 URL을 직접 호출해 이미지를 이어붙인 뒤 그 위에 경로를 그린다. **카카오맵에는 이 방식을 그대로 쓸 수 없다.**

### 3.1 카카오맵 API 제약 (확인된 사실)

- 카카오맵 API는 **가이드에 안내된 방법으로만** 사용 가능하며, 그 외의 방법으로 카카오맵 서비스/리소스에 **직접 접근하는 것은 허용되지 않는다.** → 타일 URL 직접 호출(스크래핑) 방식은 사용 불가.
- **로컬(검색) API 응답 데이터는 별도 저장 불가.** → 장소명을 DB에 캐싱하는 설계 금지, 렌더 시점 일회성 사용만.
- 2026-07-21부로 신규 API 4종 오픈: 대중교통·보행자·자전거 길찾기, **정적 지도 조회(Static Map)**.
- **무료 쿼터 정책 변경**: 개발자 계정당 **첫 번째 앱**만 무료 쿼터, **두 번째 앱부터는 Biz Wallet 연결 + 유료 설정** 필요.
- 지도 이미지 캡처/저장/영상화에 대해서는 공식 문서에 명시적 조항이 확인되지 않음 → **회색지대. 사업화 전 카카오에 서면 문의 필수.**

### 3.2 옵션 비교

| 옵션 | 방식 | 국내 지도 품질 | 영상화 적합성 | 약관 리스크 | 비용 |
|---|---|---|---|---|---|
| **① 카카오 정적 지도 API** | 중심좌표+레벨+크기로 지도 이미지 요청 → Canvas에 합성 | ★★★★★ | ★★★★ (공식 이미지 반환) | **낮음** (공식 용도) | 쿼터 내 무료 / 초과 유료 |
| ② 카카오 JS SDK 캡처 | SDK 지도 DOM을 canvas 캡처 | ★★★★★ | ★☆ (타일 CORS로 캡처 실패 잦음, 프레임 단위 제어 어려움) | **높음** | 무료 |
| ③ VWorld 배경지도 타일 | 국토부 공간정보 오픈플랫폼 타일 직접 호출 | ★★★★ (국내 한정) | ★★★★★ (원본 구조 그대로) | 낮음 (공공 오픈 API) | 무료 |
| ④ CARTO/OSM 유지 | 원본과 동일 | ★★ | ★★★★★ | 낮음 | 무료 |

### 3.3 권장 조합 (하이브리드)

```
[인터랙티브 미리보기]  카카오맵 JS SDK  — 사용자가 팬/줌하며 기간 확인
[영상 렌더링 배경]     카카오 정적 지도 API — 카메라 키프레임마다 이미지 요청
[해외 구간 폴백]       CARTO/OSM        — 국내 밖으로 나가는 경로
[장소명 라벨]          카카오 로컬 API  — 렌더 시점 조회, 저장 안 함
```

**핵심 최적화 — 정적 지도 요청 수 줄이기**
원본 파이썬처럼 "카메라 뷰포트보다 큰 이미지를 한 장 받아두고 그 안에서 팬"하는 전략을 쓴다. 30초 영상 720프레임에 대해 프레임마다 요청하면 파산이지만, **카메라 span이 임계치(예: 15%) 이상 변할 때만 재요청**하면 영상 1편당 **30~80회** 수준으로 떨어진다. 쿼터 산정의 기준이 되는 숫자이므로 스파이크에서 반드시 실측할 것.

> ⚠️ **선행 과제:** 정적 지도 API 응답 이미지를 "동영상의 배경으로 합성해 사용자가 다운로드·SNS 공유"하는 용도가 허용되는지 카카오에 공식 문의. 불허 시 → 옵션 ③(VWorld)로 렌더 레이어를 교체하고, 카카오맵은 미리보기·검색 용도로만 사용하는 구조로 전환. **이 분기 때문에 지도 레이어는 반드시 인터페이스로 추상화해서 갈아끼울 수 있게 설계한다.**

---

## 4. 제품 기획

### 4.1 포지셔닝

> **"내 발자국" — 구글 타임라인으로 만드는, 카카오맵 위의 내 1년 여행 영상**

- 타깃: 20~30대, 여행/러닝/출장 기록을 SNS에 공유하는 사람. 연말 결산 시즌에 폭발적
- 사용 시나리오: 퇴근길에 링크 접속 → JSON 업로드 → 30초 → 세로 영상 다운 → 인스타 스토리
- 핵심 약속: **설치 없음, 업로드 없음(브라우저 안에서 처리), 1분 안에 완성**

### 4.2 한국 특화 차별화 기능

| 기능 | 설명 | 우선순위 |
|---|---|---|
| **9:16 세로형 출력** | 릴스/쇼츠/스토리 규격. 원본에 없음 | P0 |
| **국내 여행 통계 오버레이** | "17개 시·도 중 12곳 · 총 8,432km · 지구 0.21바퀴" | P0 |
| **시·군·구 커버리지 맵** | 방문한 행정구역을 채색한 엔딩 스틸 (전국 229개 시군구) | P1 |
| **방문 장소명 라벨** | 카카오 로컬 API로 주요 체류지 이름 표시("강릉 안목해변") | P1 |
| **카카오톡 공유** | Kakao SDK 공유 (영상 파일이 아닌 결과 페이지 링크) | P1 |
| **사진 EXIF 연동** | 사진 자체가 입력 소스 + 지도 위 썸네일 팝업 | **P0** (입력) / P1 (썸네일) |
| **다중 소스 병합** | 사진 + 운동 GPX + 타임라인을 한 영상에 합침 | P1 |
| **BGM 트랙** | 저작권 프리 3~5곡 내장 | P2 |
| **연말 결산 모드** | "나의 2026" 프리셋 원클릭 | P2 (시즌) |

### 4.3 화면 흐름 (웹 5단계)

```
1. 랜딩          → 샘플 영상 루프 재생 + "내 기록으로 만들기"
2. 데이터 안내   → 소스 선택(타임라인 / 사진 / 운동 GPX)별 내보내기 가이드 (스크린샷)
3. 업로드        → 드래그앤드롭(다중 소스 동시 가능). 파싱 후 "2019~2026년, 총 184,203개 지점" 요약
4. 설정          → 기간(연/월/직접) · 길이 · 비율(1:1/9:16) · 카메라 · 제목 · 화질
                   + 실시간 미리보기(카카오맵 SDK)
5. 생성/공유     → 진행률 → MP4 다운로드 + 엔딩 스틸 PNG + 카톡/링크 공유
```

---

## 5. 기술 아키텍처 제안

### 5.1 스택

```
프론트엔드   Vite + React + TypeScript + Tailwind   (기존 haniagent-landing과 동일 스택)
파싱/계산    순수 TS 모듈 (원본 Kotlin/Python 알고리즘 포팅) — Web Worker에서 실행
지도         카카오맵 JS SDK(미리보기) + 정적 지도 API(렌더)  ※ MapProvider 인터페이스로 추상화
렌더링       OffscreenCanvas 2D 합성
인코딩       WebCodecs VideoEncoder(H.264) + mp4-muxer  → 폴백: ffmpeg.wasm
서버         정적 호스팅 + 카카오 API 키 프록시용 얇은 엣지 함수만 (데이터는 서버에 안 감)
```

### 5.2 모듈 매핑 (원본 → 신규)

| 원본 | 신규 모듈 | 작업 |
|---|---|---|
| `TimelineParser.kt` (371줄) | `core/sources/googleTimeline.ts` | **직역 이식.** 좌표 5종·세그먼트 3종 호환 로직 그대로 |
| (없음) | `core/sources/gpx.ts`, `core/sources/exif.ts` | **신규.** GPX 트랙, 사진 EXIF GPS 파싱 |
| (없음) | `core/sources/index.ts` | **신규.** 다중 소스 병합·정규화 |
| `LocationOutlierFilter.kt` (93줄) | `core/outlier.ts` | 직역 이식 |
| `TimelineModels.kt` | `core/journey.ts` | 직역 이식 (haversine, cum_dist, Web Mercator) |
| `JourneyTiming.kt` (96줄) | `core/timing.ts` | 직역 이식 (압축 + monotone cubic) |
| `CameraSettings.kt` (43줄) | `core/camera.ts` | 직역 이식 (3종 프리셋 상수 그대로) |
| `TimelinePainter.kt` (781줄) | `render/painter.ts` | **재작성.** 타일 스티칭 → 정적 지도 이미지 합성으로 교체. 세로형 지원 추가 |
| `Mp4Exporter.kt` (MediaCodec) | `render/encoder.ts` | **재작성.** WebCodecs로 |
| `TileRepository.kt` | `render/mapProvider.ts` | **재작성.** 인터페이스 + Kakao/VWorld/CARTO 구현체 |
| `MainActivity.kt` (1,787줄) | `pages/*`, `components/*` | 재작성 (웹 UI) |

**이식 가능 비율: 계산 로직 약 600줄은 거의 그대로, 렌더/UI 약 2,600줄은 재작성.**
MIT 라이선스이므로 코드 차용 가능하되, **README와 앱 내 정보 화면에 원저작자(@mahlernim) 표기 필수.**

### 5.3 성능 목표 및 리스크

| 항목 | 목표 | 리스크 |
|---|---|---|
| 파싱 (20만 포인트) | < 3초 | JSON 파일이 100MB+ 가능 → 스트리밍 파서 필요할 수 있음 |
| 30초 720p 인코딩 | < 40초 (데스크톱) | 모바일 사파리에서 2~3배 느림 |
| WebCodecs 지원 | Chrome/Edge ✅, Safari 17+ ✅ | **iOS 저버전 폴백 필수** → ffmpeg.wasm(5~10배 느림) 또는 서버 렌더 |
| 정적 지도 호출 | 영상당 30~80회 | 쿼터 초과 시 비용 발생. 스파이크에서 실측 |
| 메모리 | < 1GB | 타일 캐시 LRU 필수 |

---

## 6. 법률·정책 체크리스트

| 항목 | 판단 | 조치 |
|---|---|---|
| **위치정보법 (위치기반서비스사업 신고)** | 개인위치정보를 **서버로 전송·보관하지 않으면** 신고 대상에서 벗어날 여지가 크다. 서버 렌더링/계정 저장으로 가는 순간 방통위 신고 검토 대상 | **브라우저 내 처리 원칙을 아키텍처로 못 박기.** 사업화 전 변호사 확인 |
| 개인정보처리방침 | 위치 데이터 미수집 명시 | 랜딩에 게시 |
| 카카오맵 API 약관 | 렌더 이미지 영상화 가부 미확인 | **카카오 서면 문의 (선행 과제)** |
| 카카오 로컬 API | 응답 데이터 저장 금지 | 캐싱 설계 금지 |
| 원본 MIT 라이선스 | 상업 이용 가능, 저작자 표시 필요 | LICENSE 및 크레딧 표기 |
| 지도 저작권 표기 | 카카오/VWorld 각각 요구사항 상이 | 영상 하단에 출처 워터마크 고정 |
| BGM | 저작권 | 로열티프리만 사용 |

---

## 7. 로드맵

### Phase 0 — 기술 검증 (1주) ★ 여기부터 시작
- [ ] 카카오 개발자 앱 등록, 정적 지도 API 응답 실측 (해상도·좌표 정확도·응답속도)
- [ ] **카카오에 영상 합성 용도 허용 여부 서면 문의**
- [ ] WebCodecs + mp4-muxer로 "정적 이미지 위 선 그리기 → 10초 MP4" 최소 스파이크
- [ ] 실제 Timeline.json / 사진 EXIF / 건강앱 GPX 각각으로 파서 검증
- [ ] 희소 데이터(사진 EXIF)에서 카메라 파라미터가 어떻게 깨지는지 실측
- [ ] 정적 지도 호출 횟수 실측 → 비용 모델 확정
- **게이트: 위 5개 결과로 지도 레이어를 카카오 정적지도 vs VWorld로 확정**

### Phase 1 — MVP (2~3주)
- **3종 입력**(Timeline JSON / 사진 EXIF / GPX) 업로드·병합 → 기간 선택 → 미리보기 → MP4 다운로드
- 1:1 + 9:16, 카메라 3종, 길이 10~60초, 720p
- 통계 오버레이(시·도 수, 총 거리)
- 데스크톱 Chrome 우선, 모바일은 다운로드까지 검증

### Phase 2 — 차별화 (3~4주)
- 지도 위 사진 썸네일, TCX 입력
- 시군구 커버리지 엔딩 스틸, 장소명 라벨
- 카카오톡 공유, BGM
- iOS 사파리 폴백

### Phase 3 — 확장
- 연말 결산 프리셋 (10~12월 집중 마케팅)
- 앱(선택) — 웹 지표 검증 후 판단

---

## 8. 결정 사항

### 확정 (2026-08-19)
- ✅ **웹 우선** — 안드로이드 네이티브는 Kotlin 재활용률이 높지만(70%+) 설치 마찰과 아이폰 배제가 치명적
- ✅ **신규 레포 `myfootprint`** — 이 문서만 `haniagent-landing`에 남기고 구현은 전부 신규 레포
- ✅ **입력 다중화 P0** — 구글 타임라인 단일 입력은 진입장벽이 너무 높음

### 미결
1. **지도 레이어** — 카카오 정적 지도(브랜드 정합성 ↑, 약관 리스크 ○) vs VWorld(리스크 ↓, "카카오맵 기반" 네이밍 불가). Phase 0 문의 결과로 결정
2. **수익 모델** — 무료+워터마크 / 워터마크 제거 유료 / 완전 무료(포트폴리오). 정적 지도 API 유료 구간 진입 시 비용 구조가 여기에 걸린다
3. **제품 정체성** — 입력이 다중화되면서 "구글 타임라인 한국판 포크"보다 **"위치 기록이 뭐가 됐든 여행 영상으로 만들어주는 도구"**에 가까워졌다. 네이밍·랜딩 메시지를 여기에 맞출지 결정 필요

---

## 참고

- 원본 저장소: https://github.com/mahlernim/google-timeline-visualizer
- [Kakao 지도 Web API 문서](https://apis.map.kakao.com/web/documentation/)
- [카카오맵 API 신규 기능 및 무료 쿼터 운영 방식 변경 안내](https://devtalk.kakao.com/t/api-notice-on-new-kakao-map-api-features-and-free-quota-policy/150222)
- [카카오맵 신규 API 4종 및 이용 절차 개선 오픈 안내](https://devtalk.kakao.com/t/api-4/150764)
- [Kakao Developers — 카카오맵 시작하기](https://developers.kakao.com/docs/latest/ko/kakaomap/common)
- [위치정보의 보호 및 이용 등에 관한 법률](https://www.law.go.kr/%EB%B2%95%EB%A0%B9/%EC%9C%84%EC%B9%98%EC%A0%95%EB%B3%B4%EC%9D%98%EB%B3%B4%ED%98%B8%EB%B0%8F%EC%9D%B4%EC%9A%A9%EB%93%B1%EC%97%90%EA%B4%80%ED%95%9C%EB%B2%95%EB%A5%A0)
- [찾기쉬운 생활법령정보 — 개인위치정보의 보호](https://www.easylaw.go.kr/CSP/CnpClsMain.laf?csmSeq=1702&ccfNo=2&cciNo=1&cnpClsNo=1)
- [Google 타임라인 데이터 관리](https://support.google.com/maps/answer/6258979?hl=ko&co=GENIE.Platform%3DAndroid) · [기기 저장 방식 전환 안내](https://support.google.com/maps/answer/14169818?hl=ko&co=GENIE.Platform%3DAndroid)
- [Apple — 건강 데이터 공유·내보내기](https://support.apple.com/guide/iphone/share-your-health-data-iph5ede58c3d/ios)
- [Arc Timeline (iOS)](https://apps.apple.com/us/app/arc-timeline-trips-places/id1063151918) · [Google Timeline 대안 정리](https://dawarich.app/blog/best-google-timeline-alternatives-in-2026-ranked/)
